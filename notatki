W projekcie nie mozemy używać connected socket (Dial) -> bo tylko możemy wtedy rozmawiac z jedym klientem


Jak robie liscient UDP to podaje adres lokalny jak nie podam to jest to robione automatycznie

to gdzie wysylam paiety decyduje przy kazdym zadaniu uzywajac write to


Teraz uruchomienie to:

go run client.go utils.go name port
